---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section id={id} class:list={['py-[var(--section-gap)]', className]}>
  <slot />
</section>
